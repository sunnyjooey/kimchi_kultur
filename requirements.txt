# Stream 1 — data pull dependencies
# Install with: pip install -r requirements.txt

# !! pin urllib3 to v1.x — v2 breaks pytrends and some requests integrations
urllib3<2

comtradeapicall>=1.3.0
pandas>=2.0.0
requests>=2.31.0
python-dateutil>=2.8.2
pytrends>=4.9.2
openpyxl>=3.1.0         # for reading .xlsx files from KATI
beautifulsoup4>=4.12.0  # for scraping data.go.kr download links (script 03)
